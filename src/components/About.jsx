import React from "react";

import {Tilt} from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introdução</p>
        <h2 className={styles.sectionHeadText}>Visão Geral.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
       Experiência em desenvolvimento web, utilizando uma variedade de tecnologias e frameworks. Comecei com HTML, CSS e JavaScript, que me proporcionaram uma base sólida para criar interfaces de usuário interativas e responsivas.

        Com o tempo, aprofundei meus conhecimentos em Python, o que me permitiu desenvolver scripts e aplicações backend eficientes. A transição para frameworks modernos como React e Node.js foi natural, permitindo-me construir aplicações web completas e escaláveis.

        Além disso, tenho experiência com WordPress, onde desenvolvi e customizei temas e plugins para atender às necessidades específicas dos clientes. A utilização de TailwindCSS tem sido essencial para estilizar rapidamente as interfaces, mantendo um código limpo e organizado.

        Recentemente, explorei Three.js, o que me permitiu adicionar elementos 3D interativos às minhas aplicações, elevando a experiência do usuário a um novo nível.
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");